logistic_regression:
  C: 1.0
  max_iter: 1000
  class_weight: balanced

random_forest:
  n_estimators: 100
  max_depth: 10
  class_weight: balanced

xgboost:
  n_estimators: 100
  max_depth: 6
  learning_rate: 0.1
  scale_pos_weight: auto  # computed from class ratio
